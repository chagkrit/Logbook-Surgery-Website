VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key
VITE_APP_URL=https://logbook-surgery-website.vercel.app

# Server-only Google Drive OAuth settings. Configure only in Vercel.
# Never prefix these secrets with VITE_ or expose them to the browser.
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_REFRESH_TOKEN=your-persistent-refresh-token
GOOGLE_DRIVE_ACCOUNT_EMAIL=edusurgcmu@gmail.com
GOOGLE_DRIVE_BACKUP_FOLDER_ID=optional-existing-folder-id
GOOGLE_DRIVE_BACKUP_ROOT_NAME=Surgery CMU Logbook Backups

# Supabase Edge Function secrets for the Staff daily Gmail digest. The refresh
# token must include only https://www.googleapis.com/auth/gmail.send.
GOOGLE_GMAIL_CLIENT_ID=your-google-oauth-client-id
GOOGLE_GMAIL_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_GMAIL_REFRESH_TOKEN=your-persistent-gmail-send-refresh-token
GOOGLE_GMAIL_FROM_EMAIL=edusurgcmu@gmail.com
DIGEST_CRON_SECRET=generate-a-long-random-secret

# Vercel Production server environment (never VITE_). Match DIGEST_CRON_SECRET
# with the Supabase Edge Function secret. Use the project's legacy anon JWT or
# publishable API key for SUPABASE_DIGEST_API_KEY; it is not a service-role key.
CRON_SECRET=generate-a-separate-long-random-secret
SUPABASE_DIGEST_API_KEY=your-project-publishable-key
